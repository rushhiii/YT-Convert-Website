services:
  - type: web
    name: yt-convert-website
    env: node
    runtime: node
    buildCommand: |
      npm install --production=false
      npm run build
    startCommand: npm start
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
